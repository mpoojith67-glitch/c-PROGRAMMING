#include <stdio.h>
int main(){ 
int n=153,sum=0,temp=n;
while(temp)
{
int d=temp%10; 
sum+=d*d*d;
temp/=10;
}
    if(sum==n) {
printf("Armstrong\n");
}
else
{
printf("Not Armstrong\n");
}
return 0;
}

